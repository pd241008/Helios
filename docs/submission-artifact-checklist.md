# Helios Submission Artifact — Gap Checklist

> **Status: implemented (2026-09-18).** All items below are closed except
> the Zenodo DOI deposit (see `CITATION_TO_ARTIFACT.md`). Known residual
> gaps are documented in `LIMITATIONS.md` (lost run JSONs, transcribed
> tables, per-scene inventory).

Reference template: exemplar reproducibility artifact (`AnonSubmission27-CD04.zip`,
478 files — adversarial-robustness paper, anonymous venue submission). This doc
maps that template onto Helios and records what is missing.

## Status legend

✅ present · 🟡 partial · ❌ missing

## Target structure (from the exemplar)

```
.
├── README.md                  ← nav diagram + "if you're reviewing X, start here"
├── REVIEWER_GUIDE.md          ← reviewer entry point
├── REVIEW_CHECKLIST.md        ← bounded verification checklist
├── CLAIM_MAP.md               ← claim ID → evidence → script → output table
├── VERIFY.md                  ← fast verification (no full re-run)
├── REPRODUCE.md               ← full from-scratch instructions
├── REPRODUCIBILITY_LEVELS.md  ← which guarantees hold where
├── EXPECTED_OUTPUTS.md        ← expected console output + runtimes per experiment
├── LIMITATIONS.md             ← explicit boundaries
├── PROVENANCE.md              ← methodology history, superseded approaches
├── CITATION_TO_ARTIFACT.md    ← versioning + citation info
├── LICENSE
├── Makefile                   ← reproduce-*, verify, smoke targets
├── requirements lock          ← pinned environment (uv.lock ✔ already)
├── data_scripts/              ← dataset download + preprocessing
├── dataset_links.md           ← acquisition + integrity anchors
├── models/                    ← trained checkpoints, SHA-256 pinned
├── manifest/                  ← SHA-256 manifests (datasets, checkpoints)
├── experiment_manifests/      ← machine-readable per-claim manifests (C1–Cn)
├── results/                   ← archived experimental outputs (per experiment)
├── verification/              ← integrity checks, fresh-vs-archive comparison
└── docs/                      ← threat model / methodology / ADRs
```

## Gap analysis — Helios vs. exemplar

### Docs (root-level navigation layer)

| Item | Status | Notes |
|---|---|---|
| README with artifact navigation | 🟡 | `README.md` has architecture + phases; needs a "reviewing X → start here" table + tree; roadmap checkboxes are stale (all phases done but unchecked) |
| REVIEWER_GUIDE.md | ❌ | missing |
| REVIEW_CHECKLIST.md | ❌ | missing |
| CLAIM_MAP.md | ❌ | missing — the single highest-value file; see claim stubs below |
| VERIFY.md | ❌ | missing |
| REPRODUCE.md | ❌ | missing — pipeline commands exist in `Makefile` but there is no end-to-end guide incl. runtimes |
| REPRODUCIBILITY_LEVELS.md | ❌ | missing |
| EXPECTED_OUTPUTS.md | ❌ | missing — e.g. expected RMSE/R² per city, split cutoff dates |
| LIMITATIONS.md | ❌ | missing |
| PROVENANCE.md | 🟡 | material exists scattered: ADRs, postmortems, `docs/phase3-ml.md` superseded-split warning, AB-test dirs; needs consolidation |
| CITATION_TO_ARTIFACT.md | ❌ | missing |
| LICENSE | ❌ | missing |

### Reproducibility plumbing

| Item | Status | Notes |
|---|---|---|
| Pinned environment | ✅ | `ml-python/uv.lock`; Go/Scala versions pinned in docs |
| Make verify / smoke targets | ❌ | Makefile has pipeline stages but no `verify`, `smoke`, or `reproduce-*` targets |
| Checkpoint manifests (SHA-256) | ❌ | models live on F:\ (`reports_chennai_v2/lst_model.json` is gitignored); need hash manifests or in-repo copies |
| Shipped models | ❌ | `lst_model.json` (2.2 MB) not committed — must ship + pin for the artifact |
| dataset_links.md | ❌ | STAC endpoint + zoning source are in docs/Makefile, but no single integrity-anchors doc |
| experiment_manifests/ (C1–Cn JSONs) | ❌ | missing |
| verification/ scripts | ❌ | missing |

### Results (archived outputs)

| Item | Status | Notes |
|---|---|---|
| Metrics JSONs | 🟡 | `ml-python/reports*/{metrics,ensemble_metrics}.json` exist locally but gitignored + untracked; Bangalore full-res ensemble metrics not in repo at all |
| SHAP figures | 🟡 | PNGs exist locally (gitignored); need canonical copies for the paper |
| Heatmaps / trend panels | 🟡 | generated by `analysis/dataset_heatmaps.py`, outputs not archived |
| consolidation script | ❌ | no `consolidated_canonical_table.py`-equivalent aggregating per-seed/per-model results |

### Code organization (already good)

✅ modular phases (go/scala/python), ✅ Makefile orchestration, ✅ ADRs 001–005
(ADRs 004–005 match the exemplar's `docs/adr/` pattern), ✅ postmortems,
✅ tests (`geotiff_reader_test.go`, `test_data.py`).

🟡 `ml-python/_scratch_add_doy_quarantined.py` — tracked scratch file, move to
`ml-python/scripts/` or delete.

### Suggested claim IDs for Helios CLAIM_MAP

| ID | Claim (stub — fill exact numbers) | Evidence | Script | Output |
|---|---|---|---|---|
| C1 | Ensemble beats best single model on LST RMSE (both cities) | `ensemble_metrics.json` | `helios_ml/ensemble.py` | `results/{bangalore,chennai}/ensemble_metrics.json` |
| C2 | Per-city split policy (dynamic vs marker) yields leak-free test sets | ADR-005 | `helios_ml/split.py` | metrics + cutoff dates in EXPECTED_OUTPUTS |
| C3 | Zoning/LULC features materially improve prediction (SHAP) | SHAP PNGs | `helios_ml/evaluate.py` | `results/shap/*` |
| C4 | AOI-specific cloud gating preserves scene counts (cloud gate < 30%) | dual-city context doc | `cmd/check_all_aoi_cloud` | `results/scene_counts/*.json` |
| C5 | Pipeline is end-to-end reproducible from raw STAC queries | REPRODUCE.md | `Makefile` | `make all` logs |

## Priority order to close the gap

1. `CLAIM_MAP.md` + `REPRODUCE.md` + `VERIFY.md` (the core review surface)
2. `results/` layout: commit metrics JSONs + key SHAP PNGs (small, text/plain)
3. `Makefile` targets: `verify` (hashes), `smoke` (dry-run on tiny sample), `reproduce-*` per table
4. `models/` + `manifest/` — decide: commit model JSONs (lst_model.json is 2.2 MB — acceptable) or hash-pin on external drive
5. `EXPECTED_OUTPUTS.md` (exact numbers from your final runs), `LIMITATIONS.md`, `LICENSE`
6. README refresh (navigation table + up-to-date roadmap + repo-tree section)
7. `PROVENANCE.md` + `CITATION_TO_ARTIFACT.md` last
